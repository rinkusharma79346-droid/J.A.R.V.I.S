// Render-safe root entrypoint.
// If Render is configured with an empty Root Directory, `npm start` still
// launches the deployable V.A.Y.U relay server that exposes /api/call.
import './vayu-relay/server.js';
