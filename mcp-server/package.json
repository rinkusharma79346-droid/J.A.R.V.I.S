{
  "name": "jarvis-mcp-server",
  "version": "4.0.0",
  "description": "JARVIS MCP Server — Control your JARVIS Android agent from Claude Desktop, Cursor, or any MCP client via HTTP long-polling relay",
  "main": "index.js",
  "bin": {
    "jarvis-mcp-server": "./index.js"
  },
  "scripts": {
    "start": "node index.js"
  },
  "keywords": ["mcp", "jarvis", "android", "agent", "claude", "cursor"],
  "author": "JARVIS",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
