{
  "name": "jarvis-relay-server",
  "version": "6.0.0",
  "description": "JARVIS MCP Relay Server — HTTP long-polling + MCP SSE + Streamable HTTP for Claude.ai",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "uuid": "^9.0.0",
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
